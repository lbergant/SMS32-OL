; =========================================================
;                      LESSON 2: MOV ADD
; =========================================================
; In this lesson we will get to know the MOV and ADD commands
;
; 1. Try to ASSEMBLE and RUN this example
; 2. Replace one TODO at a time with code and repeat from step 1
;
; Result: Both AL and CL registers must contain value 22 (0x16)
; =========================================================
    
    MOV BL,5

    ; TODO: Store value 5 in AL

    ; TODO: Store value 17 (0x11) in CL
    ; Keep in mind that all the values in commands should be
    ; in hexadecimal notation

    ; TODO: ADD CL to AL and store it in AL (AL = AL + CL)

    ; TODO: ADD BL to CL and store it in CL (CL = BL + CL)