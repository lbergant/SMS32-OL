; =========================================================
;                   LESSON 4: Bitwise operators
; =========================================================
; In this lesson we will get to know AND OR NOT and XOR
;
; 1. Try to ASSEMBLE and RUN this example
; 2. Replace one TODO at a time with code and repeat from step 1
;
; Result: AL = 0
; =========================================================

    MOV AL,AA
    MOV BL,55
    
    ; TODO: Store value 170 in CL
    ; TODO: Store value 85 in DL

    ; TODO: Execute the AND operation on AL and BL and 
    ;       save the result to AL (AL = AL && BL)

    ; TODO: Execute the OR operation on CL and DL and 
    ;       save the result to CL (CL = CL || DL)

    ; TODO: Execute NOT operation on AL (AL = !AL)

    ; TODO: Execute XOR on AL and CL
